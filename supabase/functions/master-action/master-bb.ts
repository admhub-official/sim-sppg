import {sb,s,Caller,requireAdmin,canAccessOwner,audit,rid} from './core.ts';
export async function getBB(opt:any={}){const q=await sb.from('MASTER_BB').select('*').order('TIMESTAMP',{ascending:false});if(q.error)throw q.error;const data=q.data||[],requested=Number(opt?.page)>0||Number(opt?.pageSize)>0;if(!requested)return{success:true,data};const page=Math.max(1,Math.floor(Number(opt.page)||1)),pageSize=Math.min(100,Math.max(1,Math.floor(Number(opt.pageSize)||25))),from=(page-1)*pageSize,total=data.length;return{success:true,data:data.slice(from,from+pageSize),page,pageSize,total,hasMore:from+pageSize<total}}
export async function addBB(d:any,c:Caller){requireAdmin(c);const id=rid();const row:any={ID:id,'KODE BAHAN':s(d['KODE BAHAN']||d.KODE_BAHAN),'KATEGORI BAHAN BAKU':s(d['KATEGORI BAHAN BAKU']||d.KATEGORI_BAHAN_BAKU),'NAMA BAHAN BAKU':s(d['NAMA BAHAN BAKU']||d.NAMA_BAHAN_BAKU),'HARGA BAHAN BAKU':Number(d['HARGA BAHAN BAKU']||d.HARGA_BAHAN_BAKU)||0,SATUAN:s(d.SATUAN),DESKRIPSI:s(d.DESKRIPSI),SUPPLIER:s(d.SUPPLIER),TIMESTAMP:new Date().toISOString(),USER:c.email,UPDATE:new Date().toISOString(),YAYASAN:c.yayasan};const q=await sb.from('MASTER_BB').insert(row);if(q.error)throw q.error;await audit(c,id,'ADD','MASTER_BB',{nama:row['NAMA BAHAN BAKU']});return{success:true,message:'Bahan baku berhasil ditambahkan.',id}}
export async function updateBB(id:string,f:any,c:Caller){requireAdmin(c);const old=await sb.from('MASTER_BB').select('*').eq('ID',id).maybeSingle();if(old.error||!old.data)throw new Error('Data tidak ditemukan.');if(!(await canAccessOwner(c,old.data.USER,old.data.YAYASAN)))throw new Error('Akses data ditolak.');const allow=['KODE BAHAN','KATEGORI BAHAN BAKU','NAMA BAHAN BAKU','HARGA BAHAN BAKU','SATUAN','DESKRIPSI','SUPPLIER'];const patch:any={UPDATE:new Date().toISOString(),USER:c.email,YAYASAN:old.data.YAYASAN||c.yayasan};for(const k of allow)if(f[k]!==undefined)patch[k]=f[k];const q=await sb.from('MASTER_BB').update(patch).eq('ID',id);if(q.error)throw q.error;await audit(c,id,'EDIT','MASTER_BB',Object.keys(patch));return{success:true,message:'Bahan baku berhasil diperbarui.'}}
export async function deleteBB(id:string,c:Caller){requireAdmin(c);const old=await sb.from('MASTER_BB').select('*').eq('ID',id).maybeSingle();if(old.error||!old.data)throw new Error('Data tidak ditemukan.');if(!(await canAccessOwner(c,old.data.USER,old.data.YAYASAN)))throw new Error('Akses data ditolak.');const q=await sb.from('MASTER_BB').delete().eq('ID',id);if(q.error)throw q.error;await audit(c,id,'DELETE','MASTER_BB',{});return{success:true,message:'Bahan baku berhasil dihapus.'}}