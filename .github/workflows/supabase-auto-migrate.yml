name: Supabase Manual Migration (Legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Migration workflow consolidated
        run: |
          echo "Automatic production migrations now run in .github/workflows/supabase-auto-deploy.yml before dependent Edge Functions are deployed."
          echo "This legacy workflow is intentionally manual-only to avoid concurrent production releases."
